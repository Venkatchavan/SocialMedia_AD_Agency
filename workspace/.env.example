# ── Collection ───────────────────────────────────
APIFY_TOKEN=your_apify_token_here

# ── Vision / LLM (optional — heuristic fallback if missing) ─
GEMINI_API_KEY=
# OPENAI_API_KEY=

# ── X / Twitter (optional — stub if missing) ────
X_BEARER_TOKEN=

# ── Database ─────────────────────────────────────
# Default: SQLite at ./data/creative_os.db
# Uncomment for Postgres:
# DATABASE_URL=postgresql://user:pass@localhost:5432/creative_os

# ── Workspace defaults ───────────────────────────
DEFAULT_WORKSPACE=sample_client
DATE_RANGE_DAYS=30
MAX_ASSETS_PER_BRAND=30

# ── Logging ──────────────────────────────────────
LOG_LEVEL=INFO
